# DCL-панель OPOR (v3.6-dcl-ui) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Диалоги в стиле VBA-оригинала (меню 3×3, форма «Ввод», список опор) на чистом DCL + удобства (скрытие слоёв, возврат вида, треугольники разбивки), с фолбэком на командный ввод.

**Architecture:** Новый модуль `opor-dcl.lsp` + файл диалогов `opor.dcl` (CP1251). `opor-ui.lsp` превращается в диспетчер: DCL-путь или старый командный (переименован в `*-cmd`, не тронут по логике). Результаты формы кладутся в те же session-ключи. Спека: `docs/superpowers/specs/2026-07-06-dcl-ui-design.md`.

**Tech Stack:** AutoLISP + DCL, verify_opor.py, golden-master по логам (с формой вводы НЕ логируются — эталоны гонять с `*opor-use-dcl*` nil).

**Процесс:** git нет — без коммитов. После каждой правки — verify_opor.py. `.lsp` — UTF-8 (Edit/Write), `.dcl` — ТОЛЬКО CP1251 (писать питоном).

**Проверенные факты о коде** (не перепроверять): `opor-ensure-layer name color linetype` (geometry:28); треугольник = alist `('a pt)('az h)('b pt)('bz h)('c pt)('cz h)`, список в session `'level-triangles`; `C:OPOR` → `opor-run-safe 'opor-main`, `opor-safe-restore` зовётся и в норме, и в ошибке (errors:88-109); `opor-doc`/`opor-ms` в errors:3-6; supports-alist: ключи `'index 'name 'range 'color` (`opor-read-supports line`).

---

### Task 1: opor.dcl (CP1251)

**Files:** Create: `OPOR/opor.dcl`

- [ ] **Step 1: Записать файл питоном (Bash-heredoc), кодировка cp1251**

```python
# python - <<'EOF'  (запись opor.dcl в CP1251)
content = r'''// OPOR v3.6 DCL dialogs. CP1251! Правки — только с сохранением кодировки.

opor_mode : dialog {
  label = "OPOR";
  : row {
    : text { label = "sayan group"; }
    : text { key = "ver"; label = "Ver"; width = 24; }
  }
  : row {
    : button { key = "const";   label = "A";       width = 12; }
    : button { key = "var";     label = "B";       width = 12; }
    : button { key = "slope";   label = "Slope";   width = 12; }
  }
  : row {
    : button { key = "slopewr"; label = "%";       width = 12; }
    : button { key = "chkh";    label = "h";       width = 12; }
    : button { key = "ring";    label = "Ring";    width = 12; }
  }
  : row {
    : button { key = "wrlevl";  label = "+ 0.000"; width = 12; }
    : button { key = "clean";   label = "Очистка"; width = 12; }
    : button { key = "check";   label = "?";       width = 12; }
  }
  spacer;
  : button { key = "cancel"; label = "Отмена"; is_cancel = true; fixed_width = true; alignment = centered; }
  spacer;
  : text { label = "project@sayangroup.ru      тел. +7 495 136 6050"; alignment = centered; }
}

opor_params : dialog {
  label = "Ввод";
  : row {
    : boxed_column {
      label = "Шаги";
      : edit_box { key = "stepx"; label = "вдоль вектора"; edit_width = 8; }
      : edit_box { key = "stepy"; label = "вдоль перпендикуляра"; edit_width = 8; }
    }
    : boxed_radio_column {
      label = "Общая длина";
      : radio_button { key = "ax_vect"; label = "вдоль вектора"; }
      : radio_button { key = "ax_perp"; label = "вдоль перпендикуляра"; }
    }
  }
  : row {
    : edit_box { key = "floor";  label = "Отметка чистого пола";    edit_width = 8; }
    : edit_box { key = "radius"; label = "Радиус обозн. опоры";     edit_width = 8; }
  }
  : row {
    : boxed_column {
      label = "Покрытие";
      : row {
        : radio_button { key = "cov_d"; label = "Доска"; }
        : radio_button { key = "cov_p"; label = "Плитка"; }
      }
      : edit_box { key = "doska";  label = "толщина доски";  edit_width = 6; }
      : edit_box { key = "lag";    label = "толщина лага";   edit_width = 6; }
      : edit_box { key = "plitka"; label = "толщина плитки"; edit_width = 6; }
      : edit_box { key = "dbllag"; label = "шаг доп. лаг";   edit_width = 6; }
    }
    : boxed_radio_column {
      label = "Линейка";
      : radio_button { key = "ln_lev"; label = "Level"; }
      : radio_button { key = "ln_3d";  label = "Level 3D"; }
      : radio_button { key = "ln_pro"; label = "Level PRO"; }
    }
  }
  : boxed_row {
    label = "Крепёж";
    : popup_list { key = "flag";  label = "лаги";   edit_width = 14; }
    : popup_list { key = "ftile"; label = "плитки"; edit_width = 14; }
    : edit_box   { key = "fstep"; label = "шаг";    edit_width = 6; }
  }
  : row {
    : toggle { key = "tri";     label = "показать разбивку"; }
    : toggle { key = "hidelay"; label = "скрыть лишние слои"; }
  }
  ok_cancel;
  errtile;
}

opor_support : dialog {
  label = "Опора";
  : list_box { key = "list"; width = 46; height = 14; }
  ok_cancel;
}
'''
open(r"C:\Users\user\Documents\Таран\0_0_ по работке\плагин дмитрию\OPOR\opor.dcl",
     "w", encoding="cp1251", newline="\r\n").write(content)
print("ok", len(content))
# EOF
```

Внимание: radio_button внутри обычного `: row` в «Покрытии» — осознанно (пара
радио вне radio_column, взаимоисключение делаем руками в колбэках cov_d/cov_p:
при клике одного set_tile другого в "0").

- [ ] **Step 2: Статическая проверка** — python: прочитать cp1251, `{`==`}` по счёту, вывести OK.

---

### Task 2: Конфиг

**Files:** Modify: `OPOR/opor-config.lsp`

- [ ] **Step 1: Версия** — `"3.5-tz-p5-lag-grid"` → `"3.6-dcl-ui"` (коммент `; +DCL-панель (меню/Ввод/опора) + удобства`).

- [ ] **Step 2: После строки `*opor-keep-perp-grid*` добавить:**

```lisp
(setq *opor-use-dcl* T)                          ; nil = командный ввод (нужен для перегона эталонов: DCL не пишется в лог)
(setq *opor-layer-triangles* "линии_высот3")     ; «показать разбивку» (h_triang)
(setq *opor-visible-layers*                      ; белый список для «скрыть лишние слои»
  (list *opor-layer-contour* *opor-layer-grid* *opor-layer-supports*
        *opor-layer-support-text* *opor-layer-tiles* *opor-layer-holes*
        *opor-layer-level-lines* *opor-layer-triangles*))
```

- [ ] **Step 3: verify_opor.py** → PASS

---

### Task 3: Модуль opor-dcl.lsp

**Files:** Create: `OPOR/opor-dcl.lsp` (UTF-8, инструментом Write)

- [ ] **Step 1: Записать модуль целиком:**

```lisp
;;; OPOR DCL-панель (по образцу VBA-форм ufrm_mode/ufrm_steps/ufrm_H) + удобства

(setq *opor-form-memory* nil)          ; память формы на сессию AutoCAD (аналог arrfrm)
(setq *opor-dcl-var-p* nil)            ; режим текущего показа формы
(setq *opor-dcl-params-result* nil)

(setq *opor-dcl-lag-fasteners* '("Нет" "Level TOP" "Level Clip"))
(setq *opor-dcl-tile-fasteners* '("Нет" "Level Lastra" "Level Tile" "Level Tile Soft" "Level Lastra Soft"))

(defun opor-form-mem-get (key default / pair)
  (setq pair (assoc key *opor-form-memory*))
  (if pair (cdr pair) default))

(defun opor-form-mem-set (key value / pair)
  (setq pair (assoc key *opor-form-memory*))
  (if pair
    (setq *opor-form-memory* (subst (cons key value) pair *opor-form-memory*))
    (setq *opor-form-memory* (cons (cons key value) *opor-form-memory*)))
  value)

(defun opor-num-str (v)
  (cond
    ((not (numberp v)) "")
    ((equal v (float (fix v)) 1e-9) (itoa (fix v)))
    (t (rtos v 2 2))))

(defun opor-dcl-ready-p ()
  (and *opor-use-dcl* (findfile "opor.dcl")))

(defun opor-dcl-load (/ id)
  (setq id (vl-catch-all-apply 'load_dialog (list (findfile "opor.dcl"))))
  (if (or (vl-catch-all-error-p id) (not id) (< id 0))
    (progn (opor-log "DCL не загрузился — командный ввод.") nil)
    id))

;; ---------- меню (3x3 как sayan-форма) ----------
(defun opor-dcl-select-mode (/ id result)
  (setq id (opor-dcl-load))
  (if (not id)
    nil
    (if (not (new_dialog "opor_mode" id))
      (progn (unload_dialog id) nil)
      (progn
        (set_tile "ver" (strcat "Ver " *opor-version*))
        ;; не портировано — серым (оживёт по мере портирования S7-S12)
        (foreach k '("slope" "slopewr" "chkh" "ring" "wrlevl")
          (mode_tile k 1))
        (action_tile "const" "(done_dialog 2)")
        (action_tile "var"   "(done_dialog 3)")
        (action_tile "clean" "(done_dialog 4)")
        (action_tile "check" "(done_dialog 5)")
        (action_tile "cancel" "(done_dialog 0)")
        (setq result (start_dialog))
        (unload_dialog id)
        (cond
          ((= result 2) "const-height")
          ((= result 3) "var-height")
          ((= result 4) "clean")
          ((= result 5) "check")
          (t "exit"))))))

;; ---------- форма «Ввод» ----------
(defun opor-dcl-params-sync (/ var-p cov-d tile-f)
  (setq var-p *opor-dcl-var-p*)
  (setq cov-d (= (get_tile "cov_d") "1"))
  (setq tile-f (/= (get_tile "ftile") "0"))
  (mode_tile "floor" (if var-p 0 1))
  (mode_tile "cov_d" (if var-p 0 1))
  (mode_tile "cov_p" (if var-p 0 1))
  (mode_tile "doska" (if (and var-p cov-d) 0 1))
  (mode_tile "lag" (if (and var-p cov-d) 0 1))
  (mode_tile "plitka" (if (and var-p (not cov-d)) 0 1))
  (mode_tile "dbllag" 1)                 ; П9 (сдвоенные лаги) ещё не портирован
  (mode_tile "tri" (if var-p 0 1))
  (mode_tile "fstep" (if tile-f 0 1)))

(defun opor-dcl-cover-click (which)
  (set_tile "cov_d" (if (= which "d") "1" "0"))
  (set_tile "cov_p" (if (= which "p") "1" "0"))
  (opor-dcl-params-sync))

(defun opor-dcl-params-accept (/ sx sy r fl dsk lg plt fs line axis cov flag-idx ftile-idx err)
  (setq sx (distof (get_tile "stepx") 2))
  (setq sy (distof (get_tile "stepy") 2))
  (setq r (distof (get_tile "radius") 2))
  (setq fl (distof (get_tile "floor") 2))
  (setq dsk (distof (get_tile "doska") 2))
  (setq lg (distof (get_tile "lag") 2))
  (setq plt (distof (get_tile "plitka") 2))
  (setq fs (distof (get_tile "fstep") 2))
  (setq flag-idx (atoi (get_tile "flag")))
  (setq ftile-idx (atoi (get_tile "ftile")))
  (setq line
    (cond
      ((= (get_tile "ln_3d") "1") "3D")
      ((= (get_tile "ln_pro") "1") "PRO")
      (t "lev")))
  (setq axis (if (= (get_tile "ax_perp") "1") "perp" "vect"))
  (setq cov (if (= (get_tile "cov_p") "1") "p" "d"))
  (setq err
    (cond
      ((or (not sx) (<= sx 0.0)) "Шаг вдоль вектора: нужно число > 0")
      ((or (not sy) (<= sy 0.0)) "Шаг вдоль перпендикуляра: нужно число > 0")
      ((or (not r) (<= r 0.0)) "Радиус: нужно число > 0")
      ((and *opor-dcl-var-p* (not fl)) "Отметка чистого пола: нужно число")
      ((and *opor-dcl-var-p* (= cov "d") (or (not dsk) (<= dsk 0.0))) "Толщина доски: нужно число > 0")
      ((and *opor-dcl-var-p* (= cov "d") (or (not lg) (<= lg 0.0))) "Толщина лаги: нужно число > 0")
      ((and *opor-dcl-var-p* (= cov "p") (or (not plt) (<= plt 0.0))) "Толщина плитки: нужно число > 0")
      ((and (> ftile-idx 0) (or (not fs) (<= fs 0.0))) "Шаг креплений: нужно число > 0")
      (t nil)))
  (if err
    (set_tile "error" err)
    (progn
      (setq *opor-dcl-params-result*
        (list
          (cons 'line line)
          (cons 'step-x sx)
          (cons 'step-y sy)
          (cons 'radius r)
          (cons 'lag-axis axis)
          (cons 'tile-mode cov)
          (cons 'floor-height fl)
          (cons 'board-thickness (if dsk dsk (cdr (assoc 'board-thickness *opor-default-params*))))
          (cons 'lag-thickness (if lg lg (cdr (assoc 'lag-thickness *opor-default-params*))))
          (cons 'tile-thickness (if plt plt (cdr (assoc 'tile-thickness *opor-default-params*))))
          (cons 'lag-fastener-idx flag-idx)
          (cons 'tile-fastener-idx ftile-idx)
          (cons 'tile-fastener-step fs)
          (cons 'show-triangles (= (get_tile "tri") "1"))
          (cons 'hide-layers (= (get_tile "hidelay") "1"))))
      (foreach pair *opor-dcl-params-result*
        (opor-form-mem-set (car pair) (cdr pair)))
      (done_dialog 1))))

(defun opor-dcl-show-params (mode / id res)
  (setq *opor-dcl-var-p* (= mode "var-height"))
  (setq *opor-dcl-params-result* nil)
  (setq id (opor-dcl-load))
  (if (not id)
    nil
    (if (not (new_dialog "opor_params" id))
      (progn (unload_dialog id) nil)
      (progn
        (set_tile "stepx" (opor-num-str (opor-form-mem-get 'step-x (cdr (assoc 'step-x *opor-default-params*)))))
        (set_tile "stepy" (opor-num-str (opor-form-mem-get 'step-y (cdr (assoc 'step-y *opor-default-params*)))))
        (set_tile "radius" (opor-num-str (opor-form-mem-get 'radius (cdr (assoc 'radius *opor-default-params*)))))
        (set_tile "floor" (opor-num-str (opor-form-mem-get 'floor-height (cdr (assoc 'floor-height *opor-default-params*)))))
        (set_tile "doska" (opor-num-str (opor-form-mem-get 'board-thickness (cdr (assoc 'board-thickness *opor-default-params*)))))
        (set_tile "lag" (opor-num-str (opor-form-mem-get 'lag-thickness (cdr (assoc 'lag-thickness *opor-default-params*)))))
        (set_tile "plitka" (opor-num-str (opor-form-mem-get 'tile-thickness (cdr (assoc 'tile-thickness *opor-default-params*)))))
        (set_tile "dbllag" "0")
        (set_tile (if (= (opor-form-mem-get 'lag-axis "vect") "perp") "ax_perp" "ax_vect") "1")
        (set_tile (if (= (opor-form-mem-get 'tile-mode "d") "p") "cov_p" "cov_d") "1")
        (set_tile
          (cond
            ((= (opor-form-mem-get 'line "lev") "3D") "ln_3d")
            ((= (opor-form-mem-get 'line "lev") "PRO") "ln_pro")
            (t "ln_lev"))
          "1")
        (start_list "flag")
        (foreach item *opor-dcl-lag-fasteners* (add_list item))
        (end_list)
        (start_list "ftile")
        (foreach item *opor-dcl-tile-fasteners* (add_list item))
        (end_list)
        (set_tile "flag" (itoa (opor-form-mem-get 'lag-fastener-idx 0)))
        (set_tile "ftile" (itoa (opor-form-mem-get 'tile-fastener-idx 0)))
        (set_tile "fstep" (opor-num-str (opor-form-mem-get 'tile-fastener-step 500.0)))
        (set_tile "tri" (if (opor-form-mem-get 'show-triangles nil) "1" "0"))
        (set_tile "hidelay" (if (opor-form-mem-get 'hide-layers nil) "1" "0"))
        (opor-dcl-params-sync)
        (action_tile "cov_d" "(opor-dcl-cover-click \"d\")")
        (action_tile "cov_p" "(opor-dcl-cover-click \"p\")")
        (action_tile "ftile" "(opor-dcl-params-sync)")
        (action_tile "accept" "(opor-dcl-params-accept)")
        (action_tile "cancel" "(done_dialog 0)")
        (setq res (start_dialog))
        (unload_dialog id)
        (if (= res 1) *opor-dcl-params-result* nil)))))

;; ---------- диалог «Опора» (ufrm_H) ----------
(defun opor-dcl-choose-support (line / supports id res)
  (setq supports (opor-read-supports line))
  (if (not supports)
    (opor-ui-choose-support line)      ; нет таблицы — ручной ввод как раньше
    (progn
      (setq id (opor-dcl-load))
      (if (not id)
        (opor-ui-choose-support line)
        (if (not (new_dialog "opor_support" id))
          (progn (unload_dialog id) (opor-ui-choose-support line))
          (progn
            (start_list "list")
            (foreach s supports
              (add_list
                (strcat
                  (itoa (cdr (assoc 'index s))) ". "
                  (cdr (assoc 'name s)) "   "
                  (cdr (assoc 'range s)) "   цвет "
                  (itoa (cdr (assoc 'color s))))))
            (end_list)
            (if (>= (opor-form-mem-get 'support-list-idx 0) (length supports))
              (opor-form-mem-set 'support-list-idx 0))
            (set_tile "list" (itoa (opor-form-mem-get 'support-list-idx 0)))
            (action_tile "list"
              "(opor-form-mem-set 'support-list-idx (atoi $value)) (if (= $reason 4) (done_dialog 1))")
            (action_tile "accept"
              "(opor-form-mem-set 'support-list-idx (atoi (get_tile \"list\"))) (done_dialog 1)")
            (action_tile "cancel" "(done_dialog 0)")
            (setq res (start_dialog))
            (unload_dialog id)
            (if (= res 1)
              (nth (opor-form-mem-get 'support-list-idx 0) supports)
              nil)))))))

;; ---------- удобства ----------
;; вид (аналог x_views vb_original: запомнить в начале, вернуть в конце)
(defun opor-view-save ()
  (opor-session-set 'saved-view (list (getvar "VIEWCTR") (getvar "VIEWSIZE"))))

(defun opor-view-restore (/ v ctr size half-h half-w scr)
  (setq v (opor-session-get 'saved-view))
  (if v
    (progn
      (setq ctr (car v))
      (setq size (cadr v))
      (setq scr (getvar "SCREENSIZE"))
      (setq half-h (/ size 2.0))
      (setq half-w (* half-h (/ (car scr) (float (cadr scr)))))
      (vl-catch-all-apply 'vla-ZoomWindow
        (list (vlax-get-acad-object)
              (vlax-3d-point (list (- (car ctr) half-w) (- (cadr ctr) half-h) 0.0))
              (vlax-3d-point (list (+ (car ctr) half-w) (+ (cadr ctr) half-h) 0.0))))
      (opor-session-set 'saved-view nil))))

;; слои (замена мёртвого uv_hide_lays; гарантированный возврат через opor-safe-restore)
(defun opor-layers-hide (/ saved name obj current whitelist)
  (setq current (strcase (getvar "CLAYER")))
  (setq whitelist (mapcar 'strcase *opor-visible-layers*))
  (setq saved '())
  (vlax-for obj (vla-get-Layers (opor-doc))
    (setq name (vla-get-Name obj))
    (if (and (not (member (strcase name) whitelist))
             (/= (strcase name) current)
             (= (vla-get-LayerOn obj) :vlax-true))
      (progn
        (setq saved (cons name saved))
        (vl-catch-all-apply 'vla-put-LayerOn (list obj :vlax-false)))))
  (opor-session-set 'hidden-layers saved)
  (length saved))

(defun opor-layers-restore (/ saved layers obj)
  (setq saved
    (if (and (boundp '*opor-session*) *opor-session*)
      (opor-session-get 'hidden-layers)
      nil))
  (if saved
    (progn
      (setq layers (vla-get-Layers (opor-doc)))
      (foreach name saved
        (setq obj (vl-catch-all-apply 'vla-Item (list layers name)))
        (if (not (vl-catch-all-error-p obj))
          (vl-catch-all-apply 'vla-put-LayerOn (list obj :vlax-true))))
      (opor-session-set 'hidden-layers '())))
  (princ))

;; ---------- сборка session из формы ----------
(defun opor-dcl-fastener-name (idx items line / base)
  (if (<= idx 0)
    nil
    (progn
      (setq base (nth idx items))
      (strcat base (if (= line "3D") " 3D" "")))))

(defun opor-dcl-apply-common (vals / line)
  (setq line (cdr (assoc 'line vals)))
  (opor-session-set 'line line)
  (opor-session-set 'step-x (cdr (assoc 'step-x vals)))
  (opor-session-set 'step-y (cdr (assoc 'step-y vals)))
  (opor-session-set 'radius (cdr (assoc 'radius vals)))
  (opor-session-set 'lag-axis (cdr (assoc 'lag-axis vals)))
  (opor-session-set 'lag-fastener
    (opor-dcl-fastener-name (cdr (assoc 'lag-fastener-idx vals)) *opor-dcl-lag-fasteners* line))
  (if (> (cdr (assoc 'tile-fastener-idx vals)) 0)
    (progn
      (opor-session-set 'tile-fastener
        (opor-dcl-fastener-name (cdr (assoc 'tile-fastener-idx vals)) *opor-dcl-tile-fasteners* line))
      (opor-session-set 'tile-fastener-step (cdr (assoc 'tile-fastener-step vals))))
    (progn
      (opor-session-set 'tile-fastener nil)
      (opor-session-set 'tile-fastener-step nil)))
  (opor-session-set 'show-triangles (cdr (assoc 'show-triangles vals)))
  (if (cdr (assoc 'hide-layers vals)) (opor-layers-hide))
  T)

(defun opor-dcl-read-params (/ vals support pts)
  (setq vals (opor-dcl-show-params "const-height"))
  (cond
    ((not vals)
      (opor-log "Ввод параметров отменён.")
      nil)
    ((not (opor-ui-line-ready-p (cdr (assoc 'line vals))))
      nil)
    (t
      (setq support (opor-dcl-choose-support (cdr (assoc 'line vals))))
      (if (not support)
        (progn (opor-log "Ввод параметров отменён.") nil)
        (progn
          (opor-dcl-apply-common vals)
          (opor-session-set 'support-index (cdr (assoc 'index support)))
          (opor-session-set 'support-name (cdr (assoc 'name support)))
          (opor-session-set 'support-range (cdr (assoc 'range support)))
          (opor-session-set 'support-color (fix (cdr (assoc 'color support))))
          (setq pts (opor-ui-pick-points))
          (if pts
            (progn
              (opor-session-set 'base-point (car pts))
              (opor-session-set 'direction-point (cadr pts))
              (opor-session-set 'table-point (caddr pts))
              T)
            (progn
              (opor-layers-restore)
              (opor-log "Ввод параметров отменён.")
              nil)))))))

(defun opor-dcl-read-var-params (/ vals supports maxmark state pts)
  (setq state nil)
  (while (not state)
    (setq vals (opor-dcl-show-params "var-height"))
    (cond
      ((not vals)
        (opor-log "Ввод параметров переменной высоты отменён.")
        (setq state "cancel"))
      ((not (opor-ui-line-ready-p (cdr (assoc 'line vals))))
        (setq state "fail"))
      (t
        (setq maxmark (opor-level-max-mark *opor-session*))
        (if (and maxmark (>= maxmark (opor-round-half-even (cdr (assoc 'floor-height vals)))))
          (opor-alert
            (strcat
              "Максимальная отметка=" (opor-height-text maxmark)
              "\nУровень чистого пола=" (opor-height-text (cdr (assoc 'floor-height vals)))
              "\nУровень чистого пола должен быть выше максимальной отметки."))
          (setq state "good")))))
  (if (/= state "good")
    nil
    (progn
      (setq supports (opor-read-supports (cdr (assoc 'line vals))))
      (if (not supports)
        (progn
          (opor-log "Таблица 'Опоры' не найдена или пуста; переменная высота не сможет разложить диапазоны.")
          nil)
        (progn
          (opor-dcl-apply-common vals)
          (opor-session-set 'mode "var-height")
          (opor-session-set 'support-ranges supports)
          (opor-session-set 'tile-mode (cdr (assoc 'tile-mode vals)))
          (opor-session-set 'floor-height (cdr (assoc 'floor-height vals)))
          (opor-session-set 'board-thickness (cdr (assoc 'board-thickness vals)))
          (opor-session-set 'lag-thickness (cdr (assoc 'lag-thickness vals)))
          (opor-session-set 'tile-thickness (cdr (assoc 'tile-thickness vals)))
          (setq pts (opor-ui-pick-points))
          (if pts
            (progn
              (opor-session-set 'base-point (car pts))
              (opor-session-set 'direction-point (cadr pts))
              (opor-session-set 'table-point (caddr pts))
              T)
            (progn
              (opor-layers-restore)
              (opor-log "Ввод параметров переменной высоты отменён.")
              nil)))))))

(princ)
```

- [ ] **Step 2: verify_opor.py** — упадёт на «MISSING opor-ui-pick-points» (появится в Task 4) — это ожидаемо; зафиксировать и идти дальше. После Task 4 — PASS.

---

### Task 4: Диспетчеризация в opor-ui.lsp

**Files:** Modify: `OPOR/opor-ui.lsp`

- [ ] **Step 1: Переименовать три функции** (только заголовки defun):
  `opor-ui-select-mode` → `opor-ui-select-mode-cmd`;
  `opor-ui-read-params` → `opor-ui-read-params-cmd`;
  `opor-ui-read-var-params` → `opor-ui-read-var-params-cmd`.

- [ ] **Step 2: Общий выбор точек.** Вставить перед `opor-ui-read-params-cmd`:

```lisp
;; Общий блок кликов: начало -> направление -> точка таблицы (или nil)
(defun opor-ui-pick-points (/ base dir table-point)
  (setq base (getpoint "\nУкажите начальную точку сетки: "))
  (if (and base (not (opor-ui-base-point-ok-p (opor-2d base))))
    (setq base nil))
  (if base
    (setq dir (getpoint base "\nУкажите направление сетки: ")))
  (if (and base dir)
    (setq table-point (getpoint "\nУкажите точку вставки итоговой таблицы: ")))
  (if (and base dir table-point)
    (list (opor-2d base) (opor-2d dir) (opor-2d table-point))
    nil))
```

- [ ] **Step 3: Перевести оба `*-cmd` на helper.** В `opor-ui-read-params-cmd` заменить блок

```lisp
      (setq base (getpoint "\nУкажите начальную точку сетки: "))
      (if (and base (not (opor-ui-base-point-ok-p (opor-2d base))))
        (setq base nil))
      (if base
        (setq dir (getpoint base "\nУкажите направление сетки: ")))
      (setq table-point (getpoint "\nУкажите точку вставки итоговой таблицы: "))
      (if (and base dir table-point)
```
на
```lisp
      (setq pts (opor-ui-pick-points))
      (if pts
```
и в session-set использовать `(car pts)`/`(cadr pts)`/`(caddr pts)` вместо
`(opor-2d base)`/`(opor-2d dir)`/`(opor-2d table-point)`; из локальных убрать
`base dir table-point`, добавить `pts`. Аналогично в `opor-ui-read-var-params-cmd`
(там блок кликов внутри `(if floor-height ...)` — заменить тем же способом,
условие финала `(if (and supports floor-height base dir table-point)` →
`(if (and supports floor-height pts)`).

- [ ] **Step 4: Диспетчеры.** Добавить в конец файла (перед `(princ)`):

```lisp
;; ---------- диспетчеры: DCL или командная строка ----------
(defun opor-ui-select-mode (/ mode)
  (if (opor-dcl-ready-p)
    (progn
      (setq mode (opor-dcl-select-mode))
      (if mode mode (opor-ui-select-mode-cmd)))
    (opor-ui-select-mode-cmd)))

(defun opor-ui-read-params ()
  (if (opor-dcl-ready-p)
    (opor-dcl-read-params)
    (opor-ui-read-params-cmd)))

(defun opor-ui-read-var-params ()
  (if (opor-dcl-ready-p)
    (opor-dcl-read-var-params)
    (opor-ui-read-var-params-cmd)))
```

- [ ] **Step 5: verify_opor.py** → PASS (все opor-* резолвятся, скобки OK)

---

### Task 5: Интеграция — core, errors, levels, loader

**Files:** Modify: `OPOR/opor-core.lsp`, `OPOR/opor-errors.lsp`, `OPOR/opor-levels.lsp`, `OPOR/opor-loader.lsp`

- [ ] **Step 1: core — вид, слои, треугольники.**
  В `opor-run-const-height`: после `(setq boundary (opor-select-outer-boundary))`
  `(if boundary ...` вставить `(opor-view-save)` ПЕРЕД `(setq boundary ...)`;
  в конце успеха — после `(opor-log (strcat "MVP завершён..." ...))` добавить:

```lisp
                  (opor-layers-restore)
                  (opor-view-restore)
```

  В `opor-run-var-height`: `(opor-view-save)` перед `(setq boundary ...)`;
  после `(opor-level-triangulate *opor-session*)` добавить:

```lisp
              ;; «показать разбивку» (istriang): физические треугольники
              (if (opor-session-get 'show-triangles)
                (opor-level-draw-triangles))
```

  и в успешном финале после `(opor-log (strcat "Var завершён..." ...))`:

```lisp
                      (opor-layers-restore)
                      (opor-view-restore)
```

- [ ] **Step 2: errors — гарантированный возврат слоёв.** В `opor-safe-restore`
  первой строкой тела добавить `(vl-catch-all-apply 'opor-layers-restore nil)` —
  сработает и при ошибке, и при штатном завершении; при пустом списке — no-op.

- [ ] **Step 3: levels — отрисовка треугольников.** После `defun opor-level-triangulate`
  вставить:

```lisp
;; «показать разбивку» (chkbox_tri/istriang): closed LWPolyline на линии_высот3 (h_triang:131-133)
(defun opor-level-draw-triangles (/ triangles tri coords arr pl n)
  (setq triangles (opor-session-get 'level-triangles))
  (setq n 0)
  (if triangles
    (progn
      (opor-ensure-layer *opor-layer-triangles* 8 "Continuous")
      (foreach tri triangles
        (setq coords
          (list
            (car (cdr (assoc 'a tri))) (cadr (cdr (assoc 'a tri)))
            (car (cdr (assoc 'b tri))) (cadr (cdr (assoc 'b tri)))
            (car (cdr (assoc 'c tri))) (cadr (cdr (assoc 'c tri)))))
        (setq arr (vlax-make-safearray vlax-vbDouble '(0 . 5)))
        (vlax-safearray-fill arr coords)
        (setq pl (vla-AddLightWeightPolyline (opor-ms) arr))
        (vla-put-Closed pl :vlax-true)
        (vla-put-Layer pl *opor-layer-triangles*)
        (opor-register-created pl "triangle")
        (setq n (1+ n)))))
  n)
```

- [ ] **Step 4: loader.** В список модулей загрузчика добавить `"opor-dcl"` после `"opor-ui"`.

- [ ] **Step 5: verify_opor.py** → PASS. Проверить кодировку: `file OPOR/*.lsp` — UTF-8; `opor.dcl` — НЕ UTF-8 (CP1251/ISO-8859).

---

### Task 6: Смоук в AutoCAD (пользователь гоняет, логи читаю сам)

Тест-карта (любой тестовый чертёж, напр. эталонная геометрия):

1. `APPLOAD` loader → `OPOR`: появляется меню-диалог, серые кнопки Slope/%/h/Ring/+0.000, Ver 3.6.
2. `A` → контур/проёмы → форма «Ввод» (в Const серые: отметка, покрытие, толщины, разбивка) → OK → список опор → точки → прогон. Числа = эталонным.
3. Повторный `OPOR`/`A`: форма помнит прошлые значения.
4. `B` (Var на чертеже с отметками): всё активно, галки «показать разбивку» + «скрыть лишние слои» → во время кликов лишние слои погашены, после прогона возвращены; треугольники на «линии_высот3»; `OPORCLEAN Session` убирает и их.
5. Отмена в каждом из трёх диалогов → ничего не создано, слои/вид не тронуты.
6. Кривой ввод (шаг «abc», пустой радиус) → красное сообщение в errtile, диалог не закрывается.
7. `(setq *opor-use-dcl* nil)` → `OPOR` → старый командный ввод.
8. Вид: отзумиться в сторону перед прогоном не надо — просто убедиться, что после прогона вид вернулся к состоянию на момент старта команды.

- [ ] **Step 1: дождаться «готово», прочитать лог** (версия 3.6, «MVP завершён»/«Var завершён», отсутствие ошибок DCL)
- [ ] **Step 2: пройтись по чек-листу выше с юзером; баги → systematic-debugging**

---

### Task 7: Документация

**Files:** Modify: `HANDOFF.md`, `эталоны/README.md`, `OPOR/IMPLEMENTATION_STATUS.md`, память `opor-plugin-project.md`

- [ ] **Step 1: HANDOFF** — версия 3.6 в шапке; в раздел 2 «Как запускать»: описать панель + `*opor-use-dcl*`; в раздел 6 вычеркнуть «DCL-формы» из непортированного.
- [ ] **Step 2: эталоны/README** — в «Как перегонять» добавить шаг 0: `(setq *opor-use-dcl* nil)` перед перегоном (DCL-ввод не логируется).
- [ ] **Step 3: IMPLEMENTATION_STATUS** — строка v3.6 в «Текущее состояние».
- [ ] **Step 4: память** — П-DCL сделан: файлы, флаг, удобства, что осталось.
- [ ] **Step 5:** `mv -f *.log logs/` (если AutoCAD закрыт).
