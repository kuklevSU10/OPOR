# `?` / check_height — план реализации v3.11

Спека: `docs/superpowers/specs/2026-07-14-height-check-design.md`.

## Задачи

- [x] Разобрать `u_check_height`, `u_check_height2`, зависимости triang/get_arrblk.
- [x] Добавить отдельный `OPOR/opor-height-check.lsp`.
- [x] Подключить loader, DCL-кнопку `?`, fallback `Height` и
  `OPORHEIGHTCHECK`.
- [x] Переиспользовать триангуляцию и barycentric-плоскость порта.
- [x] Пометить XData все создаваемые блоки/полилинии.
- [x] Добавить ETS8 (`make_height_check_etalons.lsp`) и `HEIGHTDUMP`.
- [x] Пройти локальную статику и проверить CP1251 DCL.
- [x] AutoCAD LISP-smoke ETS8.
- [x] VBA A/B на чистой второй копии.
- [x] Закрепить логи и обновить статус после приёмки.

## Ворота приёмки ETS8

- лог LISP: `floor=100, уровень=10, плоскость=10, блоков проверки=4`;
- `HEIGHTDUMP`: четыре `проверкаvb3`, одна копия области и один треугольник;
- центральный блок: pt 7000,5000, scale 10, color 1, LEV 10, H 100-90;
- вершины: pt 5000,3500 / 8000,3500 / 8000,8500, scale 7,
  LEV 0/10/20, H 100-100 / 100-90 / 100-80;
- VBA A/B ETS8 (15.07): все строки `HEIGHTDUMP`, координаты, масштабы, цвета,
  атрибуты и числа полилиний совпали бит-в-бит.
- VBA даёт те же координаты, масштабы, цвета и атрибуты.
