# S6 «Сдвоенные лаги» — план реализации (v3.8-s6-dbl-lags)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Порт сдвоенных лаг из VBA 1:1 (copxlinlag + дедуп «#» из trimXL, включая квирк LENGTH-до-дедупа).

**Architecture:** Доп. семейство линий параллельно лагам строится в `opor-lags.lsp` (заглушка оживает) и вызывается из `opor-grid-build` сразу после подсчёта `grid-length`; выжившие после дедупа линии вливаются в лаговое семейство и дальше едут по существующему конвейеру (рёбра, узлы, опоры, ряды). UI — поле `dbllag` в DCL + вопрос в командном фолбэке.

**Tech Stack:** AutoLISP (Visual LISP COM), DCL (CP1251!), статическая проверка — Python-скрипт в scratchpad. **Git-репозитория нет** — вместо коммитов: статическая проверка после каждой задачи + обновление верхнего раздела `HANDOFF.md` в конце.

**Спека:** `docs/superpowers/specs/2026-07-07-s6-dbl-lags-design.md`

**ВАЖНО:** `OPOR/opor.dcl` НЕ трогаем (поле `dbllag` там уже есть, файл CP1251). Все правки — только `.lsp` (UTF-8/ANSI как есть).

---

### Task 1: Статический верификатор (baseline)

**Files:**
- Create: `<scratchpad>/verify_opor.py` (путь scratchpad — из системного промпта сессии)

- [ ] **Step 1: Написать скрипт**

```python
# -*- coding: utf-8 -*-
"""Static check OPOR/*.lsp: paren balance, defun count, unresolved opor-* calls."""
import re, sys, pathlib

ROOT = pathlib.Path(r"C:\Users\user\Documents\Таран\0_0_ по работке\плагин дмитрию\OPOR")

def strip(text):
    out, i, n, instr = [], 0, len(text), False
    while i < n:
        c = text[i]
        if instr:
            if c == "\\" and i + 1 < n: i += 2; continue
            if c == '"': instr = False
            i += 1; continue
        if c == '"': instr = True; i += 1; continue
        if c == ";":
            j = text.find("\n", i)
            i = n if j < 0 else j
            continue
        out.append(c); i += 1
    return "".join(out)

defuns, calls, bad = set(), set(), []
files = sorted(ROOT.glob("*.lsp"))
for f in files:
    txt = strip(f.read_text(encoding="utf-8", errors="replace"))
    bal = txt.count("(") - txt.count(")")
    if bal != 0:
        bad.append(f"{f.name}: paren balance {bal:+d}")
    defuns.update(m.lower() for m in re.findall(r"\(defun\s+([\w!?*<>=/+-]+)", txt, re.I))
    calls.update(m.lower() for m in re.findall(r"\(\s*(opor-[\w!?*<>=/+-]+)", txt, re.I))
    calls.update(m.lower() for m in re.findall(r"'(opor-[\w!?*<>=/+-]+)", txt, re.I))
    for m in re.findall(r"\(=\s+[^()]*:vlax-true", txt, re.I):
        bad.append(f"{f.name}: dangerous '= :vlax-true' -> {m[:60]}")

unresolved = sorted(c for c in calls if c not in defuns)
print(f"files={len(files)} defuns={len([d for d in defuns if d.startswith('opor-')])} opor-calls={len(calls)}")
for u in unresolved: bad.append(f"unresolved call: {u}")
if bad:
    print("FAIL"); [print("  " + b) for b in bad]; sys.exit(1)
print("PASS")
```

- [ ] **Step 2: Прогнать baseline**

Run: `python "<scratchpad>/verify_opor.py"`
Expected: `PASS`, defuns=252 (текущий baseline v3.7). Если FAIL до правок — остановиться, разобраться (кодировка файлов: lsp читаются как utf-8 с errors=replace; допустимо).

---

### Task 2: Конфиг

**Files:**
- Modify: `OPOR/opor-config.lsp:3` (версия) и `:83` (после `*opor-support-dedupe-tolerance*`)

- [ ] **Step 1: Версия**

Старое:
```lisp
(setq *opor-version* "3.7-ux-pass") ; +UX: автослои/автопроёмы/зум/переспрос точки/Enter-дефолты
```
Новое:
```lisp
(setq *opor-version* "3.8-s6-dbl-lags") ; S6: сдвоенные лаги (copxlinlag) 1:1, квирк LENGTH-до-дедупа
```

- [ ] **Step 2: Константы S6** — вставить после строки `(setq *opor-support-dedupe-tolerance* 1.0)`:

```lisp
(setq *opor-vba-dbl-lag-dedupe-tolerance* 10.0)  ; trimXL: maxDst = 10 (дедуп доп. лаг)
(setq *opor-dbl-lag-color* 12)                   ; copyxlinlag: newxl.color = 12
```

- [ ] **Step 3: Статика** — `python verify_opor.py` → PASS (defuns без изменений).

---

### Task 3: opor-lags.lsp — построение, дедуп, применение

**Files:**
- Modify: `OPOR/opor-lags.lsp` (замена всего файла-заглушки)

- [ ] **Step 1: Проверить, что заглушку никто не зовёт**

Run: `rg "opor-lags-not-implemented" OPOR/`
Expected: только определение в `opor-lags.lsp`. Если есть вызовы — оставить defun, дописав новые функции рядом.

- [ ] **Step 2: Записать новый opor-lags.lsp целиком**

```lisp
;;; OPOR S6: сдвоенные лаги - порт copxlinlag + дедуп "#" из trimXL.
;;; 1:1 с VBA, включая квирк: длина доп. лаг входит в LENGTH ДО дедупа
;;; (trimXL суммирует grlengCur до удаления совпавших линий).

;; Семейство доп. лаг: параллельно лаговому, шаг step, индексы диапазона bbox
;; БЕЗ нуля (VBA copyxlinlag: q начинается с 1, на базовой позиции линии нет).
(defun opor-dbl-lag-raw-lines (base axis offset-axis step axis-center half-length bbox / range)
  (setq range (opor-index-range-for-bbox bbox base offset-axis step))
  (append
    (opor-grid-build-family-range
      base axis offset-axis step axis-center half-length
      (car range) -1 *opor-layer-grid* "grid-dbl-raw")
    (opor-grid-build-family-range
      base axis offset-axis step axis-center half-length
      1 (cadr range) *opor-layer-grid* "grid-dbl-raw")))

;; trimXL: |dx|<10 и |dy|<10 покоординатно, старт-к-старту и конец-к-концу
;; (ориентация линий одного семейства у порта детерминирована, разворот не нужен)
(defun opor-dbl-lag-coincides-p (a b tol / sa ea sb eb)
  (setq sa (opor-curve-start a))
  (setq ea (opor-curve-end a))
  (setq sb (opor-curve-start b))
  (setq eb (opor-curve-end b))
  (and
    (< (abs (- (car sa) (car sb))) tol)
    (< (abs (- (cadr sa) (cadr sb))) tol)
    (< (abs (- (car ea) (car eb))) tol)
    (< (abs (- (cadr ea) (cadr eb))) tol)))

;; Полный цикл S6: построить raw, обрезать по контуру/проёмам, отфильтровать
;; короткие, покрасить в цвет 12, посчитать длину ДО дедупа (квирк VBA),
;; удалить совпавшие с основной сеткой. Возвращает (выжившие . длина-мм-до-дедупа).
(defun opor-dbl-lag-apply (base axis offset-axis step axis-center half-length bbox boundary holes lag-lines
                           / raw trimmed survivors removed len)
  (setq raw (opor-dbl-lag-raw-lines base axis offset-axis step axis-center half-length bbox))
  (setq trimmed (opor-trim-lines-by-boundaries raw boundary holes "grid-dbl"))
  (setq trimmed (opor-filter-short-grid-lines trimmed))
  (foreach line trimmed
    (vl-catch-all-apply 'vla-put-Color (list line *opor-dbl-lag-color*)))
  (setq len (opor-lines-total-length trimmed))
  (setq survivors '())
  (setq removed 0)
  (foreach line trimmed
    (if (vl-some
          '(lambda (main) (opor-dbl-lag-coincides-p line main *opor-vba-dbl-lag-dedupe-tolerance*))
          lag-lines)
      (progn
        (opor-delete-object line)
        (opor-unregister-created line)
        (setq removed (1+ removed)))
      (setq survivors (cons line survivors))))
  (opor-session-set 'dbl-lag-created (length trimmed))
  (opor-session-set 'dbl-lag-deduped removed)
  (cons (reverse survivors) len))

(princ)
```

Замечание: `line` в `foreach` не объявляется локальной — это норма для `foreach`. Все вызываемые функции существуют: `opor-index-range-for-bbox`, `opor-grid-build-family-range`, `opor-trim-lines-by-boundaries`, `opor-filter-short-grid-lines`, `opor-lines-total-length` (opor-grid/opor-trim), `opor-curve-start/end` (opor-geometry), `opor-delete-object`, `opor-unregister-created`, `opor-session-set` (opor-core/cleanup).

- [ ] **Step 3: Статика** — PASS, defuns 252→255.

---

### Task 4: Интеграция в opor-grid-build

**Files:**
- Modify: `OPOR/opor-grid.lsp:225` (локали) и `:269` (после `grid-length`)

- [ ] **Step 1: Добавить локали `dbl-step dbl-res`** в список defun (строка 225):

Старое (конец списка локалей):
```lisp
... grid edge-lines row-axis perp-lines)
```
Новое:
```lisp
... grid edge-lines row-axis perp-lines dbl-step dbl-res)
```

- [ ] **Step 2: Вставить блок S6** сразу ПОСЛЕ строки
`(setq grid-length (opor-lines-total-length lag-lines))` и ПЕРЕД
`(setq outer-segs (opor-boundary-lag-segments boundary lag-axis-vector lag-lines "outer"))`:

```lisp
      ;; S6: сдвоенные лаги (copxlinlag) - доп. семейство параллельно лагам.
      ;; Длина входит в LENGTH ДО дедупа (квирк VBA trimXL); выжившие линии
      ;; вливаются в лаговое семейство: рёбра/узлы/опоры/ряды считают их сами.
      (setq dbl-step (opor-session-get 'double-lag-step))
      (if (and (numberp dbl-step) (> dbl-step 0.0))
        (progn
          (setq dbl-res
            (if (= lag-axis "perp")
              (opor-dbl-lag-apply base perp vec dbl-step center-p half-p bbox boundary holes lag-lines)
              (opor-dbl-lag-apply base vec perp dbl-step center-v half-v bbox boundary holes lag-lines)))
          (setq grid-length (+ grid-length (cdr dbl-res)))
          (setq lag-lines (append lag-lines (car dbl-res)))
          (if (= lag-axis "perp")
            (setq p-lines (append p-lines (car dbl-res)))
            (setq v-lines (append v-lines (car dbl-res))))))
```

Почему этого достаточно (проверено по коду): `outer-segs`/`hole-segs` (рёбра) смотрят в `lag-lines` → ребро не ляжет поверх доп. лаги; `node-points`/`endpoint-points` берут `v-lines`/`p-lines` → узлы и концы доп. лаг попадают в точки опор; `lag-row-count` берёт `lag-lines` → доп. лаги = отдельные ряды; П5-удаление поперечных трогает только противоположное семейство. Grid-структура собирается ниже из уже дополненных списков.

- [ ] **Step 3: Статика** — PASS.

---

### Task 5: Командный фолбэк UI

**Files:**
- Modify: `OPOR/opor-ui.lsp` (helper после `opor-ui-read-tile-mode`, вопрос + session-set в обоих ридерах)

- [ ] **Step 1: Helper** — вставить после `opor-ui-read-tile-mode` (строка 30):

```lisp
;; S6: шаг сдвоенных лаг, 0 = выкл (VBA: tbox_2lag -> arrfrm(11))
(defun opor-ui-read-dbl-lag-step (/ v)
  (setq v (opor-ui-read-real "\nШаг сдвоенных лаг, мм (0 = нет)"
            (cdr (assoc 'double-lag-step *opor-default-params*))))
  (while (< v 0.0)
    (princ "\nНужно число >= 0.")
    (setq v (opor-ui-read-real "\nШаг сдвоенных лаг, мм (0 = нет)" 0.0)))
  v)
```

- [ ] **Step 2: Const-ридер** (`opor-ui-read-params-cmd`):
- локали: `... axis step-x step-y radius pts)` → `... axis step-x step-y radius dbl pts)`
- после строки `(setq radius (opor-ui-read-real "\nРадиус опоры, мм" ...))` вставить:
```lisp
      (setq dbl (opor-ui-read-dbl-lag-step))
```
- в блок session-set (после `(opor-session-set 'radius radius)`) вставить:
```lisp
          (opor-session-set 'double-lag-step dbl)
```

- [ ] **Step 3: Var-ридер** (`opor-ui-read-var-params-cmd`): то же самое —
- локали: добавить `dbl` (после `radius`);
- после `(setq radius ...)` вставить `(setq dbl (opor-ui-read-dbl-lag-step))`;
- в session-set блок (после `(opor-session-set 'radius radius)`) вставить `(opor-session-set 'double-lag-step dbl)`.

Вопрос идёт ДО крепежа и осей — попадает в лог AutoCAD при `*opor-use-dcl* nil` (нужно для эталонов).

- [ ] **Step 4: Статика** — PASS, defuns +1 (256).

---

### Task 6: DCL-панель

**Files:**
- Modify: `OPOR/opor-dcl.lsp:83` (mode_tile), `:92-142` (accept), `:160` (set_tile), `:297+` (apply-common)

- [ ] **Step 1: Включить поле** (строка 83):

Старое:
```lisp
  (mode_tile "dbllag" 1)                 ; П9 (сдвоенные лаги) ещё не портирован
```
Новое:
```lisp
  (mode_tile "dbllag" 0)                 ; S6 сдвоенные лаги портированы (v3.8)
```

- [ ] **Step 2: Инициализация из памяти формы** (строка 160):

Старое:
```lisp
        (set_tile "dbllag" "0")
```
Новое:
```lisp
        (set_tile "dbllag" (opor-num-str (opor-form-mem-get 'double-lag-step (cdr (assoc 'double-lag-step *opor-default-params*)))))
```

- [ ] **Step 3: Чтение и валидация в `opor-dcl-params-accept`:**
- локали: `(/ sx sy r fl dsk lg plt fs line axis cov flag-idx ftile-idx err)` → добавить `dbl`:
  `(/ sx sy r fl dsk lg plt fs dbl line axis cov flag-idx ftile-idx err)`
- после `(setq fs (distof (get_tile "fstep") 2))` вставить:
```lisp
  (setq dbl (distof (get_tile "dbllag") 2))
```
  Замечание: при пустом поле `distof` вернёт nil → сработает валидация (VBA тут падал на CInt("") — наша errtile-защита лучше, поведение расчёта не меняет).
- в `cond` валидации после строки про радиус вставить:
```lisp
      ((or (not dbl) (< dbl 0.0)) "Шаг сдв. лаг: нужно число >= 0")
```
- в список `*opor-dcl-params-result*` (после `(cons 'radius r)`) вставить:
```lisp
          (cons 'double-lag-step dbl)
```
  (память формы обновится сама — `foreach` по result уже делает `opor-form-mem-set`).

- [ ] **Step 4: Прокинуть в сессию в `opor-dcl-apply-common`** — после `(opor-session-set 'radius ...)`:

```lisp
  (opor-session-set 'double-lag-step (cdr (assoc 'double-lag-step vals)))
```

- [ ] **Step 5: Статика** — PASS.

---

### Task 7: OPORDUMP

**Files:**
- Modify: `OPOR/opor-dump.lsp` (helper перед `opor-dump-one-session`, вставка в session-блок после П5-строки, строка ~182)

- [ ] **Step 1: Helper** — вставить перед `(defun opor-dump-one-session`:

```lisp
;; S6: сводка сдвоенных лаг; пустая строка, если шаг не задан
(defun opor-dump-dbl-lag-text (/ step)
  (setq step (opor-session-get 'double-lag-step))
  (if (and (numberp step) (> step 0.0))
    (strcat
      "\n    S6 сдв. лаги: шаг=" (rtos step 2 0)
      ", создано=" (itoa (opor-dump-session-int 'dbl-lag-created))
      ", дедуп=" (itoa (opor-dump-session-int 'dbl-lag-deduped)))
    ""))
```

- [ ] **Step 2: Вставка в session-блок** — в большом `strcat`, сразу после
`(itoa (opor-dump-session-int 'perp-lines-removed))` (конец П5-строки, строка 182):

```lisp
        (opor-dump-dbl-lag-text)
```

Линии `grid-dbl` в общий свод сетки попадают сами: классификатор считает всё по `wcmatch xtype "grid*"`, а разбивку по типам печатает `type-count`.

- [ ] **Step 3: Финальная статика** — PASS; зафиксировать итоговое число defun (ожидание: 257).

---

### Task 8: Документация

**Files:**
- Modify: `OPOR/README.md` (раздел «Еще не завершено» — убрать сдвоенные лаги; upd описание opor-lags.lsp), `OPOR/IMPLEMENTATION_STATUS.md` (версия + строка S6), `HANDOFF.md` (верхний раздел)

- [ ] **Step 1:** `OPOR/README.md`: строку `- opor-tiles.lsp, opor-lags.lsp - заглушки будущих функций.` заменить на:
```markdown
- `opor-tiles.lsp` - заглушка (плитка).
- `opor-lags.lsp` - S6 сдвоенные лаги (copxlinlag + дедуп из trimXL).
```
и из «Еще не завершено» убрать пункт про сдвоенные лаги.

- [ ] **Step 2:** `OPOR/IMPLEMENTATION_STATUS.md`: версию → `3.8-s6-dbl-lags`, добавить строку: S6 сдвоенные лаги 1:1 (квирк LENGTH-до-дедупа задокументирован, см. спеку).

- [ ] **Step 3:** `HANDOFF.md` верхний раздел: код S6 написан, статика PASS (N defun), ждёт AutoCAD-прогонов S6a/S6b (см. Task 9).

---

### Task 9: AutoCAD-проверка (пользователь в цикле)

**Files:**
- Modify: `эталоны/README.md` (новая карточка S6 после сверки)

- [ ] **Step 1: Регресс S1 (шаг 0):** пользователь: эталон `эталоны/S1_lisp.dwg` → `OPORCLEAN All` → `APPLOAD` свежий loader → `(setq *opor-use-dcl* nil)` → `OPOR` со стандартным вводом S1 (контур кликом, `0,0`, `@1<0`, таблица `7000,3000`, остальное Enter; на новый вопрос «Шаг сдвоенных лаг» — Enter=0) → `DUMPALL`+`OPORDUMP`. Claude читает лог (CP1251): числа = эталон S1 после П5 (60 опор цвет 221, сеткаvb 10/30000, LENGTH 30, лаг=10), OPORDUMP БЕЗ строки S6.

- [ ] **Step 2: S6a (шаг 1000, смешанный):** та же геометрия S1. Прогон LISP (`OPOR`, шаг сдв. лаг 1000) и прогон VBA (`xx`, поле дв. лаги = 1000) от одной точки/направления. `DUMPALL` после каждого. Claude сверяет: опоры по цветам, ITOG, LENGTH, AREA, таблица — бит-в-бит LISP vs VBA. Ожидание по геометрии: доп. лаги на ±1000, ±2000 живут, ±3000 совпадает с основной (3000=5×600) и дедупится (OPORDUMP: дедуп>=1); LENGTH обоих раздут одинаково.

- [ ] **Step 3: S6b (шаг 1200, кратный):** то же, шаг 1200 у обоих. Ожидание: все доп. лаги дедупятся (1200=2×600), чертёж визуально = S1, опоры как S1, LENGTH одинаково раздут у VBA и LISP (квирк воспроизведён), OPORDUMP: создано=N, дедуп=N, выживших 0.

- [ ] **Step 4: DCL-смоук:** пользователь: `(setq *opor-use-dcl* T)` → `OPOR` → в форме поле «Шаг сдв. лаг» активно, значение помнится между прогонами, пустое/отрицательное → errtile.

- [ ] **Step 5: Эталоны и статус:** Claude записывает карточку S6 (S6a+S6b числа, пометка про квирк LENGTH и что инвариант «сеткаvb=LENGTH×1000» при S6 не действует) в `эталоны/README.md`; обновляет `HANDOFF.md` (S6 закрыт, следующий кусок переноса — плитка).

---

## Self-review (выполнен)

- Спека покрыта: конфиг (T2), UI-cmd (T5), DCL (T6), построение/дедуп/квирк (T3), интеграция+П5 (T4), дамп (T7), эталоны/доки (T8, T9). Плитка-форс-0 — отложено до порта плитки (в спеке «на будущее»).
- Имена сквозные: `double-lag-step`, `dbl-lag-created`, `dbl-lag-deduped`, `*opor-vba-dbl-lag-dedupe-tolerance*`, `*opor-dbl-lag-color*`, xtype `grid-dbl`/`grid-dbl-raw`, функции `opor-dbl-lag-raw-lines`/`opor-dbl-lag-coincides-p`/`opor-dbl-lag-apply`/`opor-ui-read-dbl-lag-step`/`opor-dump-dbl-lag-text`.
- Плейсхолдеров нет; каждый код-шаг содержит готовый код и точку вставки.
