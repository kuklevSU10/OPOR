# Карта переноса плагина «opor» с VBA на чистый LISP

> Цель: версия без «костылей» — без VBA Enabler и без Express Tools, чтобы у конечного
> пользователя (полный AutoCAD) ничего не требовалось доустанавливать.
> Подход: **сначала перенос 1:1 до полного паритета, потом правки ТЗ.**
> Допущение: у заказчика и пользователей **полный AutoCAD** (не LT). Если где-то LT — отдельный разговор (туда не встанет ни LISP, ни VBA, ни .NET).

Дата составления: 2026-06-30. Источник: `исходник/opor2.6/oporav2.6.dvb` (50 модулей, 5104 строки, 5 форм), `opor2.6.lsp`, `xetrm.lsp`.

> ✅ **СТАТУС: Фаза 0 пройдена (2026-06-30) — 7/7 PASS.** Прототип `trim_proto.lsp` подтвердил,
> что обрезку сетки по контуру (включая дуги, вогнутости и проёмы) можно сделать чистым LISP
> через `vla-IntersectWith` + ray-casting, без Express Tools. **Риск №1 снят.**

---

## Ключевой вывод

Сейчас VBA делает всё, **кроме финальной обрезки сетки**. Обрезку он делегирует LISP-у: пишет
handle контура в системную переменную `USERS5`, а `opor2.6.lsp` подхватывает и гоняет
Express-Tools `extrim` (см. `a1_mains.bas:250`). Весь огород с `USERS4/USERS5` существует
**только ради этого моста**.

В чистом LISP мост **исчезает целиком** — всё в одном процессе, обрезка становится обычным
вызовом функции. Переписывание не просто убирает VBA, оно ещё и выкидывает самую хрупкую
часть (передачу handle между процессами).

Непереносимого нет: ни Excel, ни внешних DLL, ни COM-серверов. Единственный Win API —
`GetAsyncKeyState` (отслеживание клавиши в `a_starts`), заменяется тривиально.

Остаются **два по-настоящему трудных места**: (1) замена `extrim`, (2) работа с ACAD_TABLE
и динамическими блоками. Всё прочее — объём, а не риск.

---

## Карта 50 модулей по сложности

### 🟢 Тривиально — чистая математика/массивы (~18)
`zz_functions`, `g_isPointInPolygon`, `h_triang`, `h_is_pnt_iside_tri`,
`xyz_intersect_line_plane`, `get_dist2arcs`, `f_getH_vertecs`, `f_getH_nods`, `d_constH`,
`zz_dellallssets`, `uu_cr_lay`, `uv_hide_lays`, `x_views`, `e_find_blk_color`,
`v_read_blk_slope`, `t_del_plitk`, `e_const_heght_fin`, `e_var_heght_fin`.
Нет AutoCAD API или только тривиальный — ложатся на LISP один в один.

### 🟡 Средне — выборки + геометрия + массивы, сверять с эталоном (~22)
`trimXL` (сбор/классификация линий сетки: вдоль/поперёк, дедуп сдвоенных лаг),
`copXLines`, `copxlinlag` (копирование сетки), `x_equalpointsDel`, `m_ring`, `i_clear`,
`j_slope`, `jj_slopeWR` (построение сетки уклона — ядро), `u_check_height`,
`u_check_height2`, `uu_chk_heightPL`, `u_chk_opp_reg`, `uu_chk_blk_contr`, `f_getH_bords`,
`kk_auto_levl`, `get_arrblk` (внутри a1_mains).
Логика прямая, но много обращений к модели — тут и нужны эталонные чертежи.

### 🟠 Рискованно — узкие места (см. раздел «Узкие места»)
`uv_chk_blk_lay` (18 операций с регионами), `trimPlitks`, `trrimPlitkOpp`
(обрезка плиток через Boolean-регионы), `get_tables` + запись итоговой таблицы (ACAD_TABLE),
`s_dyn_blk`, `k_write_levl`, `get_arrblk` (динамические блоки + атрибуты отметок).

### 🔵 Особые — оркестрация/формы/мост, не «верный перенос», а переосмысление (~10)
`a_starts` → станет основной командой плагина; `a1_mains` / `b2_mains` / `c3_mains` /
`b3_fin` → реструктурировать вокруг in-process обрезки (мост `USERS*` исчезает);
5 форм → **DCL** (`waitMe` схлопывается в строку статуса/`princ`); `ThisDrawing.cls` → исчезает.

---

## Узкие места (где реально может укусить)

1. ~~**Замена `extrim`** — риск №1.~~ ✅ **РЕШЕНО в Ф0** (`trim_proto.lsp`, 7/7 PASS).
   Обрезка на чистом LISP через `vla-IntersectWith` + ray-casting; готовое ядро `clip-line`
   можно брать в Ф3 как есть.
2. **ACAD_TABLE** — чтение таблицы «Опоры» (`get_tables`) и запись итоговой спецификации.
   В LISP через `vla-GetText`/`vla-SetText` — работает, но возни больше, чем в VBA.
3. **Динамические блоки** — чтение атрибутов отметок (`otmetka_oporvb`) и расстановка опор.
   В LISP через `vla-getdynamicblockproperties` — работает, но капризно.
4. **Boolean-регионы** для обрезки плиток — в LISP есть (`vla-Boolean`), проверить отдельно.

---

## Эталонные чертежи для сверки (собрать ДО старта переноса)

Для каждого фиксируем детерминированный результат: **кол-во опор по высотам, длину лаг,
итоговую таблицу.** Порт считается готовым, когда на всех совпадает с текущей VBA-версией.

1. Прямоугольный контур, постоянная высота, доска (baseline).
2. Контур **с проёмом**.
3. **Переменная высота** (блоки отметок на вершинах) + триангуляция.
4. Вариант **плитка** (доска ≠ плитка, с обрезкой).
5. Типы **Pro и 3D** (две разные таблицы опор).
6. **Сдвоенные лаги** включены.
7. Контур **со скруглениями/дугами**.

---

## Предлагаемые фазы

- ✅ **Ф0. Прототип обрезки** — ПРОЙДЕНА (7/7). Риск №1 снят, ядро `clip-line` готово.
  Детальная спецификация и результаты — см. ниже.
- **Ф1. Каркас** — команда-точка входа + DCL-формы + чтение таблицы/блоков, без расчёта.
- **Ф2. Перенос ядра** — 🟢 + 🟡 модули, сверка по эталонам 1–2.
- **Ф3. Сложные ветки** — плитка, переменная высота, проёмы, сдвоенные лаги (эталоны 3–7).
- **Ф4. Паритет** — гоняем все 7 эталонов, добиваемся совпадения. Только после этого — правки ТЗ.

---

## ТЗ Дмитрия (для справки — делается ПОСЛЕ паритета)

Половина пунктов — это правки **шаблона `.dwt`**, а не кода (диапазоны их видно из `get_tables`):
- Диапазоны высот (235‑340 и пр.), удалить «3D 0‑30», цвет 82→фиолетовый, новый визуал
  таблицы, новый размерный стиль → **редактирование `.dwt`**.
- Расчёт крепежа, сетка строго вдоль лаг, шаг опор ≠ размер плитки, сдвоенные лаги,
  название чертежа, площадь (вычесть «областиvb» из «контур») → **код**.

⚠️ Уточнить у заказчика: в тексте ТЗ диапазон Pro/3D = «205‑340», на скриншотах таблиц
выделено «235‑340». И «3D 0‑30» (текст) vs «3D 0‑35» (скриншот).

---

# Фаза 0 — прототип обрезки (детально)

## Что должен доказать прототип

Обрезать линии сетки строго по замкнутому контуру (оставить только части **внутри** контура,
с учётом **проёмов**) средствами чистого LISP — **без** `extrim` / `acet-*` / `geomcal`.

## Вход / выход

- Вход: `bnd` — замкнутая полилиния-контур (возможно с дугами); `holes` — список полилиний-проёмов
  (может быть пуст); `lines` — набор прямых LINE (слой «сеткаvb»).
- Выход: на месте каждой линии остаются только отрезки внутри `bnd` и снаружи всех `holes`;
  остальное удаляется.

## Алгоритм (intersection-based clipping)

Для каждой линии L (концы A, B):
1. Собрать все точки пересечения L с сегментами контура и проёмов методом
   `vla-IntersectWith` (объект↔объект, режим `acExtendNone`) — родной ActiveX, не Express;
   один вызов даёт все пересечения линии с полилинией, включая дуги.
2. Спроецировать точки пересечения + концы A, B на параметр `t` вдоль линии, отсортировать.
3. Между соседними `t_i, t_{i+1}` взять середину M. Если M **внутри контура**
   (point-in-polygon по `bnd`) И **не внутри** ни одного проёма → отрезок «оставить».
4. Удалить исходную линию, создать заново только «оставленные» отрезки.

Point-in-polygon уже есть — `g_isPointInPolygon` (переносится тривиально), переиспользуем.

## Почему так, а не native `_.TRIM` / EXTRIM

- `extrim` — сам Express Tool, его и убираем.
- Драйвить native `_.TRIM` с забором для сотен линий + проёмов муторно и нестабильно
  (зависит от зума/регена).
- Пересечение прямых с полилинией — простая детерминированная математика; `IntersectWith`
  делает тяжёлую часть, point-in-polygon уже есть. Полностью тестируемо, без внешних зависимостей.

## Главный вопрос, который проверяем в Ф0

Корректно ли `vla-IntersectWith` работает на: (1) дугах контура, (2) касании в вершине,
(3) линии, коллинеарной сегменту контура. Если на вырожденных случаях сбоит — добавить ручной
расчёт пересечений (`inters`) как фолбэк для прямых сегментов.

## Тестовый набор (Ф0 «зелёная», если все проходят)

| #  | Геометрия | Ожидаемо |
|----|-----------|----------|
| T1 | Линия целиком внутри прямоугольника | остаётся целиком |
| T2 | Линия целиком снаружи | удаляется |
| T3 | Линия пересекает контур один раз (один конец внутри) | обрезается до точки входа |
| T4 | Линия через **вогнутый** контур (вход-выход-вход) | два отрезка, дырка посередине |
| T5 | Линия через контур **с проёмом** | разрыв на проёме |
| T6 | Линия касается вершины | без «мусорных» нулевых отрезков |
| T7 | Линия вдоль **дуги/скругления** контура | корректное пересечение с дугой |
| T8 | Реальная сетка (~50–200 линий) на контуре из `пример шаблона для плагина.dwg` | совпадение с текущим `extrim` |

## Как сверять с эталоном (старый плагин)

1. Открыть тестовый чертёж, запустить **текущий** плагин (команда `xx`) — отработает `extrim`.
   Зафиксировать: число линий на «сеткаvb» после обрезки и их суммарную длину
   (быстрый LISP-замер: выбрать слой → сумма `vla-get-Length`).
2. На таком же исходном чертеже (до обрезки) запустить прототип.
3. Сравнить число отрезков и суммарную длину. Допуск < 0.1 мм по суммарной длине.
   Чисто на T1–T7 + совпадение на T8 → Ф0 закрыта, главный риск снят.

## Форма поставки

Один самодостаточный `trim_proto.lsp` с командой `c:trimproto`, который либо строит тестовую
геометрию T1–T7 и печатает PASS/FAIL, либо работает на выделении (контур + проёмы + линии) для T8.
Грузится через APPLOAD, больше ничего не нужно.

## Скелет ядра (иллюстрация, не финал)

```lisp
;; обрезать линию ln по контуру bnd (vla-object) и списку проёмов holes-list
(defun clip-line-by-contour (ln bnd holes-list / a b ipts ts segs)
  (setq a (vlax-curve-getStartPoint ln)
        b (vlax-curve-getEndPoint   ln))
  ;; 1) все пересечения с контуром и проёмами
  (setq ipts (get-intersections ln bnd))          ; vla-IntersectWith -> список точек
  (foreach h holes-list
    (setq ipts (append ipts (get-intersections ln h))))
  ;; 2) параметры t вдоль линии: концы + пересечения, отсортировать, убрать дубли
  (setq ts (sort-unique (cons 0.0 (cons 1.0 (mapcar '(lambda (p) (param-on-line a b p)) ipts)))))
  ;; 3) интервалы, чьи середины внутри контура и вне проёмов -> оставить
  (foreach iv (pairs ts)
    (setq m (mid-on-line a b (car iv) (cadr iv)))
    (if (and (pt-inside-p m bnd)
             (not (some '(lambda (h) (pt-inside-p m h)) holes-list)))
        (setq segs (cons (list (pt-on-line a b (car iv)) (pt-on-line a b (cadr iv))) segs))))
  ;; 4) пересоздать оставленные отрезки, удалить исходную линию
  (foreach s segs (make-line (car s) (cadr s)))
  (vla-delete ln))
```

(`get-intersections` оборачивает `vla-IntersectWith` + разбор safearray в список точек;
`pt-inside-p` — это перенесённый `g_isPointInPolygon`.)

## Грубая оценка Ф0

~150–250 строк, но именно тут вся неопределённость. День-два с отладкой на вырожденных случаях.
Если `IntersectWith` ведёт себя прилично — быстро; если для дуг придётся писать свои
пересечения — дольше.
