# UX-пасс OPOR (v3.7-ux-pass) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Удобный поток A/B: авто-скрытие чужих слоёв, автопроёмы, зум на контур, подсветка контура + переспрос при промахе точкой, Enter-дефолты для точки/направления/таблицы.

**Architecture:** Всё поверх готовых механизмов v3.6 (layers-hide/restore, view-save/restore, error-handler). Общая точка — `opor-ui-pick-points` (используется и DCL-, и командным путём). Спека: `docs/superpowers/specs/2026-07-07-ux-pass-design.md`.

**Tech Stack:** AutoLISP; verify_opor.py после каждой правки; git нет — без коммитов; `.lsp` UTF-8 (Edit), `opor.dcl` CP1251 (только питоном).

**Проверенные факты:** `opor-bbox` (geometry:94), `opor-select-outer-boundary` (geometry:172), `opor-select-holes` (geometry:206 — остаётся, перестаёт вызываться), `opor-polyline-closed-p` (:111), `opor-polyline-vertices` (:127), `opor-point-inside-boundary-p pt boundary` (:142), `opor-point-on-curve-p pt curve tol` (:151), `opor-2d` (:70), `opor-polyline-line-edge-pairs` (grid, возвращает 2D-пары прямых рёбер), `opor-layers-hide/restore` и `opor-view-save/restore` (opor-dcl.lsp), error-handler в errors:88.

---

### Task 1: Конфиг + geometry (detect-holes, zoom)

**Files:** Modify: `OPOR/opor-config.lsp:3`, `OPOR/opor-geometry.lsp` (после opor-select-holes)

- [ ] **Step 1: Версия** — `"3.6-dcl-ui"` → `"3.7-ux-pass"` (коммент `; +UX: автослои/автопроёмы/зум/переспрос точки/Enter-дефолты`).

- [ ] **Step 2: В geometry после `opor-select-holes` добавить:**

```lisp
;; UX v3.7: проёмы не выбираются руками — все замкнутые полилинии на областиvb,
;; все вершины которых внутри контура или на нём
(defun opor-detect-holes (boundary / holes obj inside)
  (setq holes '())
  (vlax-for obj (opor-ms)
    (if (and
          (= (vla-get-ObjectName obj) "AcDbPolyline")
          (= (strcase (vla-get-Layer obj)) (strcase *opor-layer-holes*))
          (opor-polyline-closed-p obj)
          (not (equal obj boundary)))
      (progn
        (setq inside T)
        (foreach pt (opor-polyline-vertices obj)
          (if (and inside
                   (not (opor-point-inside-boundary-p (opor-2d pt) boundary))
                   (not (opor-point-on-curve-p (opor-2d pt) boundary *opor-point-tolerance*)))
            (setq inside nil)))
        (if inside (setq holes (cons obj holes))))))
  (setq holes (reverse holes))
  (opor-log (strcat "Проёмов найдено: " (itoa (length holes))))
  holes)

;; UX v3.7: центрирование экрана на контуре (+10% отступ)
(defun opor-zoom-to-boundary (boundary / bbox ll ur dx dy)
  (setq bbox (opor-bbox boundary))
  (if bbox
    (progn
      (setq ll (car bbox))
      (setq ur (cadr bbox))
      (setq dx (* 0.1 (max 1.0 (- (car ur) (car ll)))))
      (setq dy (* 0.1 (max 1.0 (- (cadr ur) (cadr ll)))))
      (vl-catch-all-apply 'vla-ZoomWindow
        (list (vlax-get-acad-object)
              (vlax-3d-point (list (- (car ll) dx) (- (cadr ll) dy) 0.0))
              (vlax-3d-point (list (+ (car ur) dx) (+ (cadr ur) dy) 0.0)))))))
```

- [ ] **Step 3: verify_opor.py** → PASS

---

### Task 2: Авто-дефолт точки/направления (grid)

**Files:** Modify: `OPOR/opor-grid.lsp` (перед `opor-lag-row-count`)

- [ ] **Step 1: Добавить:**

```lisp
;; UX v3.7: авто-дефолт — вершина длиннейшего прямого ребра + направление вдоль него;
;; контур целиком из дуг -> первая вершина + ось X
(defun opor-grid-default-base-dir (boundary / best best-len len first-pt)
  (setq best nil)
  (setq best-len 0.0)
  (foreach pair (opor-polyline-line-edge-pairs boundary)
    (setq len (distance (car pair) (cadr pair)))
    (if (> len best-len)
      (progn (setq best pair) (setq best-len len))))
  (if best
    (list (car best) (cadr best))
    (progn
      (setq first-pt (opor-2d (car (opor-polyline-vertices boundary))))
      (list first-pt (opor-v+ first-pt (list 1000.0 0.0 0.0))))))
```

- [ ] **Step 2: verify_opor.py** → PASS

---

### Task 3: pick-points — подсветка, переспрос, Enter-дефолты (ui)

**Files:** Modify: `OPOR/opor-ui.lsp` (заменить `opor-ui-pick-points` целиком, добавить хелперы подсветки перед ним)

- [ ] **Step 1: Заменить текущий `opor-ui-pick-points` на:**

```lisp
;; UX v3.7: подсветка контура на время кликов + снятие из error-handler
(defun opor-ui-highlight (obj on)
  (vl-catch-all-apply 'vla-Highlight (list obj (if on :vlax-true :vlax-false)))
  (vl-catch-all-apply 'vla-Update (list obj)))

(defun opor-unhighlight-session (/ obj)
  (setq obj
    (if (and (boundp '*opor-session*) *opor-session*)
      (opor-session-get 'highlighted-object)
      nil))
  (if obj
    (progn
      (opor-ui-highlight obj nil)
      (opor-session-set 'highlighted-object nil)))
  (princ))

;; UX v3.7: клики точек — контур подсвечен, промах = переспрос (не отмена),
;; Enter = авто-дефолты, Esc = штатное прерывание через *error*
(defun opor-ui-pick-points (/ boundary defaults base dir table-point done bbox ll ur dx)
  (setq boundary (opor-session-get 'outer-boundary))
  (setq defaults (opor-grid-default-base-dir boundary))
  (opor-ui-highlight boundary T)
  (opor-session-set 'highlighted-object boundary)
  (setq done nil)
  (while (not done)
    (setq base (getpoint "\nНачальная точка сетки <Enter = угол у длинного ребра>: "))
    (cond
      ((not base)
        (setq base (car defaults))
        (setq done T))
      ((or (opor-point-inside-boundary-p (opor-2d base) boundary)
           (opor-point-on-curve-p (opor-2d base) boundary *opor-point-tolerance*))
        (setq base (opor-2d base))
        (setq done T))
      (t
        (princ "\nТочка вне контура — кликните внутри подсвеченного контура или Enter для авто."))))
  (setq dir (getpoint base "\nНаправление сетки <Enter = вдоль длинного ребра>: "))
  (if dir
    (setq dir (opor-2d dir))
    (setq dir (opor-v+ base (opor-v- (cadr defaults) (car defaults)))))
  (setq table-point (getpoint "\nТочка вставки итоговой таблицы <Enter = справа от контура>: "))
  (if table-point
    (setq table-point (opor-2d table-point))
    (progn
      (setq bbox (opor-bbox boundary))
      (setq ll (car bbox))
      (setq ur (cadr bbox))
      (setq dx (max 1000.0 (* 0.15 (- (car ur) (car ll)))))
      (setq table-point (list (+ (car ur) dx) (cadr ur) 0.0))))
  (opor-ui-highlight boundary nil)
  (opor-session-set 'highlighted-object nil)
  (list base dir table-point))
```

Примечание: pick-points теперь ВСЕГДА возвращает список из трёх точек; отмена —
только Esc (уходит в *error*, там возврат слоёв/вида/подсветки). Ветки
`(if pts ... (progn (opor-log "...отменён") nil))` у вызывающих остаются — они
просто не срабатывают в обычном потоке, трогать их не надо.

- [ ] **Step 2: verify_opor.py** → PASS

---

### Task 4: Поток в core + error-handler

**Files:** Modify: `OPOR/opor-core.lsp` (оба run-*, opor-main), `OPOR/opor-errors.lsp:88`

- [ ] **Step 1: Const.** Было:

```lisp
      (opor-view-save)
      (setq boundary (opor-select-outer-boundary))
      (if boundary
        (progn
          (opor-session-set 'outer-boundary boundary)
          (setq holes (opor-select-holes))
```
Стало:
```lisp
      (opor-view-save)
      (opor-layers-hide)                      ; UX: чужие слои гаснут до выбора контура
      (setq boundary (opor-select-outer-boundary))
      (if boundary
        (progn
          (opor-zoom-to-boundary boundary)    ; UX: центрируем экран на контуре
          (opor-session-set 'outer-boundary boundary)
          (setq holes (opor-detect-holes boundary))  ; UX: проёмы автоматом
```

- [ ] **Step 2: Var.** Было:

```lisp
      (opor-view-save)
      (setq boundary (opor-select-outer-boundary))
      (if (not boundary)
        nil
        (progn
          (opor-session-set 'mode "var-height")
          (opor-session-set 'outer-boundary boundary)
          (setq holes (opor-select-holes))
```
Стало:
```lisp
      (opor-view-save)
      (opor-layers-hide)                      ; UX: чужие слои гаснут до выбора контура
      (setq boundary (opor-select-outer-boundary))
      (if (not boundary)
        nil
        (progn
          (opor-zoom-to-boundary boundary)    ; UX: центрируем экран на контуре
          (opor-session-set 'mode "var-height")
          (opor-session-set 'outer-boundary boundary)
          (setq holes (opor-detect-holes boundary))  ; UX: проёмы автоматом
```

- [ ] **Step 3: Хвост opor-main.** Было:

```lisp
    (t (opor-log "Команда отменена.")))
  (princ))
```
Стало:
```lisp
    (t (opor-log "Команда отменена.")))
  ;; UX: слои возвращаются на ЛЮБОМ пути выхода (отмена контура/формы и т.п.)
  (opor-layers-restore)
  (princ))
```

- [ ] **Step 4: error-handler.** Было:

```lisp
(defun opor-error-handler (msg)
  (vl-catch-all-apply 'opor-layers-restore nil) ; вернуть слои и при ошибке/ESC
  (opor-safe-restore)
```
Стало:
```lisp
(defun opor-error-handler (msg)
  (vl-catch-all-apply 'opor-layers-restore nil) ; вернуть слои и при ошибке/ESC
  (vl-catch-all-apply 'opor-unhighlight-session nil) ; снять подсветку контура
  (opor-safe-restore)
```

- [ ] **Step 5: verify_opor.py** → PASS

---

### Task 5: Убрать галку «скрыть лишние слои» из формы

**Files:** Modify: `OPOR/opor.dcl` (питоном, CP1251!), `OPOR/opor-dcl.lsp`

- [ ] **Step 1: opor.dcl** — питоном: прочитать cp1251, удалить строку
`    : toggle { key = "hidelay"; label = "скрыть лишние слои"; }`, записать cp1251,
проверить баланс скобок `{`==`}`.

- [ ] **Step 2: opor-dcl.lsp** — три удаления:
  1. строка `(set_tile "hidelay" (if (opor-form-mem-get 'hide-layers nil) "1" "0"))`;
  2. в `opor-dcl-params-accept` строка `(cons 'hide-layers (= (get_tile "hidelay") "1"))`;
  3. в `opor-dcl-apply-common` строка `(if (cdr (assoc 'hide-layers vals)) (opor-layers-hide))`.

- [ ] **Step 3: verify_opor.py** → PASS; `file OPOR/opor.dcl` → по-прежнему не-UTF-8 (CP1251).

---

### Task 6: Смоук в AutoCAD (пользователь)

Чек-лист: A на чертеже с проёмами и чужими слоями → чужие слои погасли до выбора
контура → после клика по контуру зум на него → «Проёмов найдено: N» в командной
строке (вопрос выбора исчез) → форма без галки слоёв → точки: контур подсвечен,
клик мимо → подсказка и переспрос, Enter на всех трёх → авто-дефолты → прогон →
слои и вид вернулись, числа обычные. B — то же + области высот. Esc посреди
точек → слои/вид/подсветка вернулись.

- [ ] **Step 1:** дождаться «готово», прочитать лог, сверить.

---

### Task 7: Документация

- [ ] **Step 1:** `HANDOFF.md` — версия 3.7 в шапке и «Статус сейчас»; раздел 2:
  новый поток (автослои/автопроёмы/зум/Enter-дефолты).
- [ ] **Step 2:** `эталоны/README.md` — процедура: вопрос «выберите проёмы» исчез
  (проёмы автоматом); prompts точек с Enter-дефолтами (клики/координаты как раньше).
- [ ] **Step 3:** память проекта — абзац v3.7.
